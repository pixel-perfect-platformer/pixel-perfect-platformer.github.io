{
  "rules": {
    "leaderboards": {
      "$levelId": {
        "scores": {
          ".read": true,
          ".write": "auth != null",
          "$scoreId": {
            ".validate": "newData.hasChildren(['uid', 'username', 'levelIndex', 'time', 'jumps', 'timestamp']) && newData.child('time').isNumber() && newData.child('time').val() >= 0.5 && newData.child('time').val() <= 3600 && newData.child('jumps').isNumber() && newData.child('jumps').val() >= 0 && newData.child('jumps').val() <= 10000 && newData.child('uid').val() == auth.uid"
          }
        }
      }
    },
    "levels": {
      ".read": true,
      ".write": "auth != null"
    },
    "official_levels": {
      ".read": true,
      ".write": "auth != null && (auth.token.email == 'krisvih32@platformer.local' || root.child('admins').child(auth.uid).exists())"
    }
  }
}
